// src/components/Header.tsx
import React from 'react';

const Header: React.FC = () => {
  return (
    // JSX goes here
    <header>
      {/* content */}
    </header>
  );
};

export default Header;
